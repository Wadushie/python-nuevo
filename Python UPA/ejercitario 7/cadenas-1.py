# Dado un texto t, determinar cuántas veces aparece una letra en el mismo. La letra debe ser
# ingresada por el usuario.
string = input('Ingrese una palabra dentro de la cadena :) = ')
longitud = len(string)
print('La cadena [', string, '] tiene:',longitud, 'letras')